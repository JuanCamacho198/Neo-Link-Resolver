
        let links = document.querySelectorAll('a');
        links.forEach(function(link) {
            if(link.href.includes('acortalink.net') && (!link.hasAttribute('taget') || link.getAttribute('target') != '_blank'))
                link.setAttribute('target', '_blank');
        });
        